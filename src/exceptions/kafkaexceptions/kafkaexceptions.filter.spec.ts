import { KafkaExceptionsFilter } from './kafkaexceptions.filter';

describe('KafkaexceptionsFilter', () => {
  it('should be defined', () => {
    expect(new KafkaExceptionsFilter()).toBeDefined();
  });
});
